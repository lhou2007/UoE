#include "divider.h"

int int_divide(int a, int b) {
  int quotient = 0;//changed to ...=0
  // Add your code here...
  int quotient = 0;
  while (a >= b * quotient) {
    quotient++;
  quotient --;
  }
  return 0;
}
